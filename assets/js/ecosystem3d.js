
(()=>{const canvas=document.querySelector('[data-ecosystem-canvas]');if(!canvas||!window.ECOSYSTEM_NODES)return;const ctx=canvas.getContext('2d');const hud=document.getElementById('nodeHud');const N=window.ECOSYSTEM_NODES;let w=0,h=0,dpr=1,rx=-.18,ry=.42,drag=false,lastX=0,lastY=0,hover=-1,auto=true;const pts=[];const gold=Math.PI*(3-Math.sqrt(5));N.forEach((n,i)=>{if(i===0){pts.push({x:0,y:0,z:0,node:n,hub:true});return}const j=i-1,m=N.length-1,y=1-(j/(m-1))*2,r=Math.sqrt(Math.max(0,1-y*y)),phi=gold*j;pts.push({x:Math.cos(phi)*r*235,y:y*235,z:Math.sin(phi)*r*235,node:n,hub:n.kind==='hub'})});function size(){const r=canvas.getBoundingClientRect();dpr=Math.min(devicePixelRatio||1,2);w=r.width;h=r.height;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}new ResizeObserver(size).observe(canvas);size();function rot(p){let x=p.x,y=p.y,z=p.z;let cy=Math.cos(ry),sy=Math.sin(ry),cx=Math.cos(rx),sx=Math.sin(rx);let x1=x*cy+z*sy,z1=-x*sy+z*cy;let y1=y*cx-z1*sx,z2=y*sx+z1*cx;return{x:x1,y:y1,z:z2}}function proj(p){const r=rot(p),dist=650,scale=dist/(dist-r.z);return{x:w*.5+r.x*scale,y:h*.50+r.y*scale,z:r.z,s:scale}}function draw(t){ctx.clearRect(0,0,w,h);const grad=ctx.createRadialGradient(w*.5,h*.5,20,w*.5,h*.5,Math.min(w,h)*.55);grad.addColorStop(0,'rgba(33,117,255,.10)');grad.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=grad;ctx.fillRect(0,0,w,h);if(auto&&!drag&&!matchMedia('(prefers-reduced-motion: reduce)').matches)ry+=.0015;const P=pts.map(proj);ctx.lineWidth=1;for(let i=1;i<P.length;i++){const a=P[0],b=P[i];ctx.strokeStyle=pts[i].node.kind==='research'?'rgba(44,217,255,.16)':pts[i].node.kind==='venture'?'rgba(155,124,255,.13)':'rgba(255,255,255,.14)';ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}const order=P.map((p,i)=>[p,i]).sort((a,b)=>a[0].z-b[0].z);order.forEach(([p,i])=>{const n=pts[i].node,focus=i===hover;let r=(i===0?10:n.kind==='hub'?6:4.2)*Math.max(.72,Math.min(1.4,p.s));ctx.beginPath();ctx.arc(p.x,p.y,r+(focus?5:0),0,Math.PI*2);ctx.fillStyle=focus?'rgba(255,255,255,.15)':'rgba(0,0,0,0)';ctx.fill();ctx.beginPath();ctx.arc(p.x,p.y,r,0,Math.PI*2);ctx.fillStyle=i===0?'#eef8ff':n.kind==='research'?'#2cd9ff':n.kind==='venture'?'#9b7cff':'#d9ff69';ctx.shadowBlur=focus?22:10;ctx.shadowColor=ctx.fillStyle;ctx.fill();ctx.shadowBlur=0;if((focus||i===0||n.kind==='hub')&&w>540){ctx.font=(focus?'600 ':'500 ')+(focus?'13px':'10px')+' ui-sans-serif,system-ui';ctx.fillStyle=focus?'#fff':'rgba(220,239,250,.75)';ctx.fillText(n.title,p.x+r+8,p.y-8)}});if(!drag){let mx=mouse.x,my=mouse.y,best=-1,bd=22;P.forEach((p,i)=>{const dd=Math.hypot(mx-p.x,my-p.y);if(dd<bd){bd=dd;best=i}});if(best!==hover){hover=best;updateHud(best)}}requestAnimationFrame(draw)}const mouse={x:-999,y:-999};function updateHud(i){if(!hud)return;if(i<0){hud.innerHTML='<span>DRAG / TOUCH TO ROTATE</span><b>Invariant Computing</b><p>Click a node to open its public site.</p>';return}const n=pts[i].node;hud.innerHTML='<span>'+n.status.toUpperCase()+'</span><b>'+n.title+'</b><p>'+n.body+'</p>'}canvas.addEventListener('pointerdown',e=>{drag=true;auto=false;lastX=e.clientX;lastY=e.clientY;canvas.setPointerCapture(e.pointerId)});canvas.addEventListener('pointermove',e=>{const r=canvas.getBoundingClientRect();mouse.x=e.clientX-r.left;mouse.y=e.clientY-r.top;if(drag){ry+=(e.clientX-lastX)*.006;rx+=(e.clientY-lastY)*.006;rx=Math.max(-1.2,Math.min(1.2,rx));lastX=e.clientX;lastY=e.clientY}});canvas.addEventListener('pointerup',e=>{drag=false;try{canvas.releasePointerCapture(e.pointerId)}catch{}setTimeout(()=>auto=true,2200)});canvas.addEventListener('pointerleave',()=>{mouse.x=-999;mouse.y=-999;if(!drag){hover=-1;updateHud(-1)}});canvas.addEventListener('click',()=>{if(hover>=0&&!drag)window.open(pts[hover].node.url,'_blank','noopener')});requestAnimationFrame(draw)})();
